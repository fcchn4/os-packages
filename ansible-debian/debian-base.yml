---
  - hosts: all
    become: True
    roles:
      - debian-base
    vars:
      debian_user: freddy
      debian_version: buster
      debian_host: 10.172.0.65
      debian_private_key_file: ~/.ssh/demo-ssh
      debian_port: 22