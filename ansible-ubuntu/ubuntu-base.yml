---
  - hosts: all
    become: True
    roles: 
      - ubuntu-base
    vars:
      ubuntu_user: freddy
      ubuntu_version: focal
      ubuntu_host: 10.172.0.60    # IP Address or localhost
      ubuntu_private_key_file: ~/.ssh/demo-ssh
      ubuntu_port: 22